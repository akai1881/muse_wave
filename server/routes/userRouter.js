const Router = require('express');
const router = new Router();


router.post('/signup',);
router.post('/login',);
router.get('/auth', (req, res) => {
    res.json({msg: 'fuck huli not working'});
});

module.exports = router;

